# Y A M B A R

hack: &hack Hack Nerd Font:pixelsize=16
time: &time Hack Nerd Font:pixelsize=14
bg_default: &bg_default {stack: [{background: {color: 1b668fff}}, ]}
bar:
  height: 24
  location: top
  font: *hack
  spacing: 2
  margin: 0
  left-margin: 0
  layer: bottom
  foreground: fcfcfcff
  background: 232627ff

  left:
    - river:
        anchors:
          - base: &river_base
              left-margin: 8
              right-margin: 9 
              default: {string: {text: , font: *hack}}
              conditions:
                id == 1: {string: {text: 1, font: *hack}}  
                id == 2: {string: {text: 2, font: *hack}}  
                id == 3: {string: {text: 3, font: *hack}}  
                id == 4: {string: {text: 4, font: *hack}}  
                id == 5: {string: {text: 5, font: *hack}}  
                id == 10: {string: {text: "scratchpad", font: *hack}}  
                id == 11: {string: {text: "work", font: *hack}}  

        content:
          map:
            on-click: 
              left: sh -c "riverctl set-focused-tags $((1 << ({id} - 1)))"
              right: sh -c "riverctl toggle-focused-tags $((1 << ({id} -1)))"
              middle: sh -c "riverctl toggle-view-tags $((1 << ({id} -1)))"
            conditions:
              state == urgent:
                map:
                  <<: *river_base
                  deco: {background: {color: D08770ff}}
              state == focused:
                map:
                  <<: *river_base
                  deco: *bg_default
              state == visible && ~occupied:
                map:
                  <<: *river_base
              state == visible && occupied:
                map:
                  <<: *river_base
                  deco: *bg_default
              state == unfocused:
                map:
                  <<: *river_base
              state == invisible && ~occupied: {empty: {}}
              state == invisible && occupied:
                map:
                  <<: *river_base
                    #deco: {underline: {size: 3, color: ea6962ff}}
  center:
    - clock:
        content:
          - string: {text: "{date}", font: *time, right-margin: 5}
          - string: {text: "{time}", font: *time}
